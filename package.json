{
  "name": "cloudera-manager-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP stdio server for Cloudera Manager REST API",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --loader ts-node/esm src/server.ts",
    "test:unit": "node --test --loader ts-node/esm test/config.test.ts test/cmClient.test.ts",
    "test": "npm run test:unit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke:read": "node --loader ts-node/esm src/scripts/smokeRead.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
